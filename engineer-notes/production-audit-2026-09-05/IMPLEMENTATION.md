# Reliability implementation ledger

Goal opened 5 September 2026 at David's request. Completion requires all audit issues corrected and an independent final Astra adversarial review with no P2-or-higher findings. Hardware claims require hardware evidence. This ledger does not replace those gates.

## Baseline

- Audited source: `64cbcafd9d046c65f89934588cd1d13d29a17351`.
- Audit/evidence checkpoint: `b072fd6`.
- GitHub main verified to match the audited source at implementation start.
- User authorizes implementation, PR creation, resolving reviewer disagreements, and merging only after Astra approval. Ask David only at a true impasse.

## Work and gates

| Item | Implementation | Independent review | Verification / remaining work |
|---|---|---|---|
| F1 bounded non-UI microphone ingress | Merged PR #6, `83ee2a5` | Astra approved `bdc2402` | Callback/UI blocking regression passed; bounded ingress verified |
| F6 original capture timestamps, shared clock, streaming converter | Merged PR #6, `83ee2a5` | Astra approved corrected `262b5f8` and merge head `bdc2402` | Jitter, partition, stereo conversion and UI blocking tests passed; hardware alignment remains |
| F8 independent starvation detection and genuine completion deadline | Merged PR #5, `fe9030c` | Astra approved `da6779c` | Off-UI detection and typed completion tests passed; native lifecycle assessed separately |
| F7 authoritative local recording independent of inference | Merged PR #7, `1240cee` | Astra approved `f0be283`; merge tree identical | 170 Swift + 64 Python tests passed, including all six reviewer regressions |
| F2/F3/F4/F5 unified preview/recording supervision and honest recovery | Merged PR #9, `25f69c9` | Astra approved author `3d5abae` and root integration `0c7bb87` | 193 combined Swift tests passed; reviewer independently ran actual lifecycle tests |
| F8 complete lifecycle bounds and artifact outcomes | Merged PR #11 `b849ffe`; startup owner `2ec8cd4`, root `ad93826` | Astra approved `2ec8cd4` and root `ad93826` | Author 241 tests; root combined 243 Swift tests and Release passed |
| F9 session-scoped screenshot/video integrity | Merged PR #11 `b849ffe`; artifact chain through `58cac24`, source lease `df7e3bf`, capture intent `69d0990` | Astra approved these frozen slices | Independent source/recovery/screenshot deadline tests passed; clean and hosted 244 Swift + 83 backend tests and Release passed |
| Offline mode/assets and Merge handoff | Offline merged PR #8, `480dfdf`; external Merge instructions updated with backup | Astra approved `63d8b57` and revised Merge contract | 77 Python tests, Release build, actual ASR+Senko synthetic speech under OS network denial passed; signed-package qualification and shared archive ownership remain |
| Distribution, build identity, CI, clean install/rollback | PR #10 merged `83770b2`; corrected clean build records locked runtime/toolchain | Astra approved initial slice and correction `2e6f9e4` | Signed package, clean machine and rollback qualification remain |
| Duration/media integrity semantics | Merged PR #11; `5d90000`, `75fc958`, `07486dc`: media-derived duration, per-session outcomes, all indexed source validation | Astra approved finalizer through `5d1ffeb` | Prior loss/inference failure plus clean Resume, missing source, old wall-clock and ASR overshoot regressions |
| Whole-project adversarial Astra audit | Additional broad pass in progress | Final integrated candidate still requires a fresh review | Required result: no remaining P2+ findings |
| Release soak/hardware matrix | Not started | Pending | Actual wall time/routes and controlled signals, no substitute from unit tests |

## Design decisions

- Keep independent system and mic capture; a mic replacement must not stop system capture or MP4.
- Use a shared source clock created before capture, with original buffer timestamps. Explicitly journal sleep transitions; do not relabel delayed buffers with delivery time.
- Swift owns source PCM and a committed-byte manifest. Python reads only committed prefixes in the new mode; it must never open source files for writing or delete them. Existing framed-stdin mode remains supported for compatibility/tests.
- Authoritative events reach disk before UI delivery. Meter messages are disposable; transcript/control events are not.
- A timeout must report uncertainty and retain ownership correctly. Cancelling a Task is not proof that a framework operation ended.
- Preserve existing meetings/readers; schema changes need migration/defaults and tests.

## Review record

- PR #5: https://github.com/DavidFarrell/muesli/pull/5 — Astra-approved head `da6779c575c878f5945fe66936a0e4a30065f225`; merged as `fe9030cdc5cce9bbf9bf8230d5fcc4f869799d6e` on 5 September 2026.
- PR #6: https://github.com/DavidFarrell/muesli/pull/6 — Astra-approved head `bdc24029bf2979afe21c260c75d0711e3ab2e071`; merged as `83ee2a5f92f59a1c8093c20a0c1c7fa8b5102130` on 5 September 2026.
- F7 review identified inference-failure false success, unreported source gaps/overlap, failed-resume session rollback, torn UTF-8 replay and overlapping journal owners. Corrected in `01c501a`; reviewer independently verified all five. The final follow-up found stale published durability during a subsequent blocked synchronization; `f0be283` publishes each successful committed prefix immediately and adds a 602-event stalled-sync regression. Astra approved `f0be283`; merged PR #7.
- Review rejection is treated as work to resolve, not as a reason to waive the release gate. No hardware soak, clean-install, signed distribution or final whole-project approval is claimed.

## Additional merged and external work

- PR #7: https://github.com/DavidFarrell/muesli/pull/7 — approved source durability tree `f0be283`, identical merge candidate `ebe798d`; merge `1240cee6c709ba030e368ee1882f875c0a5d2eb6`.
- PR #8: https://github.com/DavidFarrell/muesli/pull/8 — approved offline tree `63d8b57`, identical candidate `690a587`; merge `480dfdf3c7e8f1f46ec1c4d28dad8c9bd00bff4f`.
- PR #9: https://github.com/DavidFarrell/muesli/pull/9 — approved combined lifecycle tree `0c7bb87`, identical candidate `9f28ddf`; merge `25f69c98570ce2d8762a0676f70940340d9f1fff`.
- External Merge skill: independently reviewed and hash-guarded update applied to its existing Obsidian skill location; original saved as `SKILL.md.pre-reliability-2026-09-05`. Approved contract hash `acae0f06158d54c1a4e9f30e47af10e8bee12d99da22a49f648dd31ac9208342`; installed entrypoint hash after reviewer-recommended example polish `9826846d12d3a0780ee6dea96ac4de0c7cb6995405b5f5ba4dd355d60eb8aa02`. No client recording processed or trashed. Cleanup remains blocked while Muesli runs until a shared ownership mechanism exists.
- Actual installed-runtime qualification: a 14.034-second generated Samantha voice fixture passed ASR and Senko under `sandbox-exec` with `(deny network*)`; output reproduced the test speech with one small transcription variation. A first fixture generated without the speech service permission was empty, failed inference and is not counted as a successful run. This is runtime evidence on this Mac, not a signed-release or clean-machine result.
- Pending F9 review corrections include resume media extent, missing indexed sources, truthful MP4 timing, durable failure events, screenshot callback ownership, zero-audio video scope, and transactional transcript replacement. Individual subprocess completion `fd7283b` is approved; integrated persistence remains under review.

## Transaction and startup integration checkpoint

- Transcript provenance and recoverable replacement: Astra approved author chain `94b7b04`, `19e5b87`, `3718449`, `60ab720`. Review corrections cover stalled disk ownership, crash during rollback cleanup, stale viewer loads and edits while a prior result is unobserved. Independent focused regressions passed.
- Root finalizer `07486dc` replays only the durable journal prefix, reduces events off UI, validates every indexed source, and commits transcript text, JSONL, metadata and source inventory together under one folder owner. Five-second waits report pending; original ownership survives and actual completion publishes the result. Full 235 Swift tests passed. Astra approved the finalizer plus `5d1ffeb` publication correction; 42 independent focused tests passed.
- Combined startup integration is `ad93826`; actual start preparation is bounded independently of stalled storage and retains abandoned resources until actual close. It preserves earlier session outcomes before Resume. Author full 241 tests/Release passed; root combined 243 Swift tests and Release passed; 14 independent startup/deadline tests passed.
- External Merge reference received the separately reviewed transaction-journal safeguard; installed reference hash is now `481fe326fbde7f094c737e194c49b00147a54b3f860e3f5220e83bf20ba7982b`. Entrypoint unchanged. No meeting was processed or trashed.
- PR #10: https://github.com/DavidFarrell/muesli/pull/10. Initial local run passed 83 backend tests, 224 Swift tests and Release compilation in a fresh Python environment. It reused existing download caches and inherited a user-level uv policy, so it was not a clean-machine qualification. The first hosted run failed closed at `--locked` when that undeclared policy was absent; the corrective build configuration `2e6f9e4` is independently approved and the hosted rerun is in progress. A separate empty-cache run with hostile local settings passed 77 backend tests, 193 Swift tests and Release compilation on the isolated CI branch.

## Verified merged integration and remaining release work

- PR #10 merged as `83770b25847f9d5ac17f2ee208a37466ca579dcd` after hosted run `33986873285` passed (4m16s).
- PR #11 merged as `b849ffec819ec692895d49745dd22a31b9b01ec1` after hosted run `33987194040` passed (2m43s). The candidate tree was identical to clean local qualification commit `e19b9cb65ecdbdf34cb0deb8b14bddc2d8ea6278`: 83 backend tests, 244 Swift tests and Release compilation, fresh package cache, recorded `source_dirty=false`.
- Catalog/metadata ownership is a separate correction under review. It covers remaining UI-thread directory/file work, orphan recovery, truthful metadata edits and deletion ownership.
- Build traceability is an independently approved slice through `c2d627a`: embedded source/build identity and actual observed runtime identity, with private content excluded from default diagnostics.
- Packaged-runtime tools are under review. A relocated standalone Python/locked dependency/minimal decoder payload passed real ASR+Senko on generated speech with cold caches, read-only payload, OS network denial and original staging/cache access denied. A separate cold parallel Numba/OpenMP probe passed. The static native audit's independent review found architecture and path-traversal gaps; these have regression corrections. This remains development qualification, not a production sandbox or distributable app.
- A supported no-network XPC/helper prototype is being implemented and tested independently. The installed app remains untouched. Developer ID/notarization, the exact signed candidate's cold offline operation, and the real eight-hour/100-route hardware qualification remain uncompleted gates.


## Catalog, traceability and packaged runtime checkpoint

- PR #12 merged as `0a2d08f9524a96efaf911c6f03ce8b6a9cf2200d` after hosted run `33989032358` passed. Independently approved packaging corrections include ARM64/native-header validation and canonical system-library paths. The clean local candidate passed 83 Python, 244 Swift and 9 native packaging fixture tests plus Release compilation.
- Build/runtime provenance chain through `c2d627a` received independent Astra approval after 12 Python and 6 Xcode provenance tests; root integrated it as `7552ac7`, `914d686`, `606a22a`.
- Catalog and metadata author commits `8f4ef3a` and `528c1d6` are integrated as `956dfa2` and `4a0694b`. Actual artifact leases protect delayed native writers even after a deadline. Fresh history publication avoids overwriting later edits with an older finalizer result. Independent combined review is pending.
- Root Stop integration retires edit admission before its first suspension and transfers accepted names into the retained finalizer, applying the latest accepted values after fresh disk metadata and journal replay. Duplicate Stop is rejected while finalizing. Combined 271 Swift tests passed, including failure of the preceding metadata edit; clean combined qualification is pending.
- The strict signed inference proof remains isolated from the app. Actual network-denial, peer rejection and blocked-output cancellation tests passed. Actual inference crashed inside llvmlite's executable-memory allocator under hardened runtime with `allow-jit`. Automatic approval review rejected adding `allow-unsigned-executable-memory`; it has not been applied. An independently reviewed, service-only one-entitlement proposal is prepared at `/private/tmp/muesli-inference-xpc/release/inference-service/unsigned-memory-proposal.md` and requires explicit user authorization. Its approval for user review is not approval to apply it or a production runtime qualification.


## Verified CI recovery and workflow integrity checkpoint

This checkpoint supersedes the pending PR #13/catalog statements above; earlier entries preserve their original evidence scope.

- PR #13 merged as `51df37d4a0ba8943a8f357e4211acb79320b50ce` after independently approved `7ec6b43` and hosted run `33991150640` passed. Separate post-merge main run `33991396173` passed: 265 Swift tests, 95 Python tests, 9 packaging fixtures and Release compilation, with `source_dirty=false`.
- Failure emails were reconciled with actual runs: `33986113920` was the resolver-policy failure fixed in PR #10; post-PR #11 main run `33987356123` exposed the screenshot timing test; PR #13 run `33990439269` exposed short-sleep screenshot/metadata notice assertions. Tests now observe actual completion with bounded deadlines. The screenshot one-shot timeout reports directly under its request identity without consuming its sole event by checking a separately injected clock, while retaining the native owner. Astra independently approved these corrections. Passing PR checks do not substitute for separate post-merge main results.
- Export author `1272f8d` was independently approved after 10 focused tests; root integration `d04e316`. A fresh persisted selected-folder snapshot publishes text and JSONL together into a new folder and retains pending operations until actual completion.
- Attachment authors `6ecf02d` and `d9f7b21` were independently approved after 27 tests; root `8917dfc` and `66b9b63`. Unique names, bounded worker-side decoding, content/source identity, recoverable manifests and directory-descriptor deletion preserve originals across failure and path replacement.
- Preview authors `b4100cf` and `154fb15` were independently approved after 8 tests; root `c90d340`, `433fdab`, selected-folder notice `503d37e`. Bounded owned reads replace interface-thread loading and preserve legacy alignment uncertainty.
- Backend admission authors `1db62c2`, `d0eefa8`, `6845511` were independently approved for normal Stop/timeout/cancellation after 48 tests; root `03d9e4c`, `2cb8f1c`, `d900fde`. Native launch, journals, scopes and actual pipe/process close retain one owner away from the interface. A separate surviving-child-after-app-crash gap remains open and is being corrected.
- Independent Astra combined review approved `d900fde8f53cfe9aaedd3f2e1b2686b723568d1a`. Clean verification at that exact source passed 307 Swift tests, 95 Python tests, 9 packaging fixtures and Release compilation with `source_dirty=false`. Publication was subsequently rejected by automatic approval review; no associated PR or merge exists for this workflow branch yet.
- Shared archive-exclusion ownership remains in progress. The external Merge skill still requires the app to remain closed during cleanup; no automatic archive qualification or user-source Trash operation is claimed.
- The helper-only executable-memory proposal has a stable copy at `engineer-notes/production-audit-2026-09-05/unsigned-memory-proposal.md`. Explicit user approval remains pending, the entitlement remains unapplied, and actual signed helper inference has not passed.
- Final fresh whole-project Astra review, signed-package qualification, clean install/update/rollback and actual eight-hour/100-route hardware evidence remain open completion gates.


## Local shared-ownership and crash-survival checkpoint

- Shared ownership author chain `f338053`, `706746b`, `c9b7baa`, `e2153c1`, `570768d` received independent approval after 47 actual tests in a separate checkout. Shared access spans actual native/source/artifact/backend/log operations and terminal saves; canonical transactions use a separate exclusive lock. An added real second-process regression failed before the final reference-transfer correction and passed afterward. The final identified Release build passed with a clean source state.
- Receipt foundation `4fe482a`, `21604a1` received independent approval after 12 tests. The reviewer reproduced a case-insensitive path alias that could falsely place outputs outside the source. The corrected validator compares opened ancestor and file identities, requires the actual persisted receipt to match, rejects duplicate physical outputs, supports split-note groups and retains ambiguous cleanup outcomes. This is reusable validation, not an enabled archive operation.
- Surviving-child author `8f25b24` received independent approval after 27 Python tests and 25 Swift tests. Python takes its own shared existing-folder locks before entry-point source access, validates fixed native identities, and retains them through process exit. Tests kill the parent before and after child admission; stale paths cannot be recreated and actual catalog deletion remains excluded until the child exits. New app entry flags prevent silently using an older backend that ignores the protocol.
- Combined local integration `f176fae4d6df0ba776e1a03b1ab1924201248f39` received independent seam approval and clean verification: 333 Swift tests, 122 Python tests, 9 packaging fixtures and Release compilation, `source_dirty=false`. This preserves the actual-close handoff correction, native child identities and required flags. These changes are local; no hosted verification or merge is claimed.
- Publication of the earlier reviewed workflow branch was rejected twice by automatic approval review, including after read-only verification of the existing public repository and exact source/test/audit payload. Specific user approval for publishing that payload is pending; no workaround publication was attempted. This is separate from the still-unapproved helper executable-memory exception.
- Newly confirmed remaining integrity work: batch results can become stale if Resume adds a source during reprocessing, and duplicate final results are currently accepted. An app-owned source snapshot and replacement validation are in progress. Modern in-app speaker-identification screenshots also need the committed artifact ledger instead of legacy-only folder discovery.
- External automatic cleanup remains disabled while the app runs. Native source eligibility, per-source/stream processing evidence, exact official-output edit evidence, qualified app participation, durable pending receipt and actual Trash result/reconciliation remain outstanding. The installed external skill is unchanged in this checkpoint; no client source was processed or trashed.
- The full goal remains open: remaining source corrections, final fresh Astra review, signed helper/package and physical hardware qualification still apply.

## Additional adversarial corrections and local integration

- Complete observed byte-edit evidence `42d2ba6` received independent root approval after 12 Swift and 13 Python tests in a separate checkout. Strict Swift 6 and clean identified Release passed. Root integration `c677e40`. This proves exact output transformation, not semantic redaction appropriateness or cleanup permission.
- Batch source/replacement integrity `f1af203` received independent Astra approval after 37 tests, plus clean identified Release. Root integration `9e72e16` preserves the newer required child-lease flag. A retained prelaunch snapshot and fresh final transaction validation reject appended/changed sources, duplicate final results and changed reviewed names; selected-stream/source timing is checked before canonical replacement.
- Exhaustive source inventory `56e4fd3` received independent Astra approval after 37 source-inventory/receipt/access tests, plus clean identified Release. Root `45b3ccb`. Actual APFS recovery-journal case aliases are refused, while root path aliases resolve to the already-owned physical identity. All files and empty directories are inventoried under retained exclusive access. Source semantics and move authorization remain separate.
- Screenshot chain `2454695`, `0c602f0`, `f70ad25` received independent Astra approval after 46 tests, including the unchanged ancestor-swap regression that initially failed. Root `447bb00`, `fc969e7`, `924881e`. Modern indexed artifacts retain source/time/UUID provenance through descriptor-relative bounded reads; private transcript/model/name debug prints were removed; suggested names require the same source content and reviewed-name basis at presentation, confirmation and fresh disk mutation.
- Clean combined verification at exact `924881eba4cb140f415c0ffc60648bed884eb141` passed 393 Swift tests, 135 Python tests, 9 packaging fixtures and Release compilation, with `source_dirty=false`. Evidence: `/private/tmp/muesli-screenshot-batch-integration-verification`. All changes remain local; no hosted verification or merge is claimed.
- The additional whole-project Astra pass reproduced another P2 in source-picker thumbnails: three structured-timeout waits remained suspended after two seconds until their native callbacks were released. Correction `75daadc` received independent Astra approval after 13 callback/completion tests and a clean identified Release. Root integration `4b28a31` retains each request until both invocation and callback settle, bounds waits, rejects duplicate/old callbacks, retires picker generations and bounds shared content enumeration. The combined verification above predates this additional correction.
- The broad pass also confirmed missing sleep/wake implementation: the host media clock can omit sleep without a source loss record, and fresh post-wake frames can conceal the interruption from the periodic health check. A typed source-scoped pause record and generation-fenced mic/system/preview reconciliation are being implemented. Actual sleep/route hardware qualification remains separate.
- Native source-semantic eligibility and explicit per-source/per-stream processing evidence are being implemented independently. No automatic archive entry point, pending-move journal or external skill update is enabled. The final fresh integrated Astra review, signed helper/package, clean installation/update/rollback and physical eight-hour/100-route test remain incomplete.
- Both specific approval requests remain pending: publication of the earlier reviewed workflow PR to the public repository, and the isolated helper-only unsigned-executable-memory entitlement. Neither action has been retried or bypassed without the requested user response.

## Source, sleep and native processing review checkpoint (6 September)

- Source eligibility `aa541ed` → `d198d4d` is independently approved and integrated as `a6c44c8`, `788ceef`. The reviewer demonstrated that an extra real WAV with an unlisted suffix bypassed the first root-media filter; a closed allowlist now retains all unknown root material. Author 63 focused tests, changed-source strict Swift 6 and clean identified Release passed. Reviewer 66 tests and the unchanged real-WAV reproduction passed. This proves closed, fully indexed, loss-free recorded evidence under actual retained leases; it does not itself authorize cleanup.
- Sleep/wake `bcafad64` → `d9c90a1` is independently approved and integrated as `2b65b87`, `1a97173`. The reviewer reproduced a new binding during an observed but unfinished sleep cycle escaping the first implementation. Such a binding now durably records unknown continuity. Original source identity remains immutable, acknowledgments precede source writes, and old wake callbacks cannot restart a new generation. Author full 402-test baseline, 43 focused correction tests and clean identified Release passed; reviewer 44 tests passed. Actual hardware sleep/wake and route qualification remains open.
- Native processing validator `db7f12b`, `512fb6a`, `4488636` is independently approved and integrated as `14bc556`, `c5b6755`, `f53c16c`. Reviewer 42 native tests include both unchanged impossible-count reproductions and the actual Python producer's synthetic fixture, with PCM/manifest/WAV hashes independently derived. Author 39 tests, changed-source strict Swift 6 and identified Release passed. Recovery frame hashes come from retained original PCM, including an independent full-source hash check. This is a parser and source adapter; actual native completion authority, final output/source revalidation and move ownership remain separate work.
- Callback correction `b74e78a` is independently approved and integrated as `05b7733`. Preserving the already-required Sendable contract through MicEngine storage lets every app body pass Swift 6/default MainActor/complete strict concurrency/warnings-as-errors. Reviewer checked 73 app source files plus a freshly generated build identity, with no diagnostics. Test-target migration and physical runtime behavior are separate.
- The Python processing producer is not yet approved: an independent reviewer reproduced altered newly generated recovery/normalization files being admitted before their expected digest was bound. Its correction is in progress. The independently verified native consumer rejects such mismatched inputs, but the producer still needs its own correction and review.
- Normal Quit bypassing durable shutdown is a confirmed open issue from the broad review. Implementation tracks actual accepted source/artifact/backend/canonical work and uses a cooperative termination decision; a wait deadline must never imply permission to quit. No actual installed-app Quit or recording was performed during these tests.
- The latest full combined qualification is still `924881e` (393 Swift/135 Python/9 packaging/Release); the later approved additions need a new full combined run. There is no new remote PR, merge, signed-runtime qualification, native automatic archive entry point or final whole-project approval. Both specific approval requests remain pending.

## Native completion, producer and cooperative Quit checkpoint (6 September)

This supersedes the pending producer/Quit/combined-test statements above.

- Native completion `1389546` is independently approved and integrated as `01a5885`: only actual successful process exit, complete stdout drain and original resource closure produce the non-Codable source-bound processing proof. Author 46 tests, whole-app strict concurrency and identified Release passed; independent 54 tests passed, including the stalled actual resource-close test. Serialized receipt/result fields cannot supply this proof.
- Python producer `356a051` → `b531349` → `6c5a198` is independently approved and integrated as `65a11b4`, `a88d7e3`, `1971a39`. Independent 187 tests include all 184 frozen tests plus three original adversarial reproductions. Generated normalization/recovery WAVs are now bound to immutable producer observations before model admission; deletion/header-read and format races fail fatally instead of falling through a best-effort path. No real client recording was processed.
- Clean combined qualification at exact `1971a3987e258cafac6a4a3c2f631db4ac8e92b9` passed 452 Swift, 184 Python, 9 packaging tests and Release compilation, `source_dirty=false`. Evidence: `/private/tmp/muesli-processing-power-combined-verification`.
- Cooperative Quit `8ec2437` → `fdea477` is independently approved and integrated as `21fe6ed`, `447d0bd`. Accepted actual recording/save/backend owners block automatic termination until they finish. Cancel Quit reopens admission without restoring retired Start intent. Reviewer 46 focused tests include the unchanged immediate-Cancel/prepared-Start race. The integration review confirmed native processing evidence construction and ownership are preserved; the one source conflict retained both the Quit admission guard and the original evidence-budget/accumulator handling.
- The first full post-Quit run exposed a cross-language fixture missing the new real ShutdownWorkRegistry compile dependency. Local correction `5c62571` changes only that source list. Fresh clean verification at `5c62571` passed 466 Swift, 184 Python, 9 packaging tests and Release compilation. Evidence: `/private/tmp/muesli-quit-combined-v2-verification`. The correction does not replace real ownership with test stubs.
- Durable move journal, saved note/image provenance and local archive coordinator/transport are separately under adversarial review. Review reproduced both a substituted-marker overwrite and a reused socket-descriptor ownership race; their corrections must be approved before integration. There is no activated automatic archive entry point and no actual Finder Trash call.
- Final fresh integrated Astra review, signed helper/package qualification, installation/update/rollback and physical eight-hour/100-route/sleep qualification remain open. Both specific publication and isolated-helper entitlement approval requests remain pending; no such action has been retried or bypassed.

## Archive component integration checkpoint

- Journal `56619c2` → `78b2c2a` is independently approved and integrated as `91eafa3`, `d6d1725`. The reviewer reproduced an unexpected marker being overwritten and reported as saved. Immutable initial intent now survives a removed marker; atomic exchange preserves and validates displaced evidence. Independent 20 native tests passed, including staging/symlink substitution. No actual move permission is conferred by this journal.
- Saved outputs `ecf3f8e` is independently approved and integrated as `688d2ac`: full actual-byte edit replay, exact source/stream/speaker declarations, native event-log binding, bounded saved image references and copied-image catalog matching. Independent 50 tests passed. Rendered visibility, names and redaction meaning remain declarations rather than machine-proven semantic facts.
- Local workflow `1b809db` → `e05e98d` is independently approved and integrated as `a67b27b`, `e13dab2`. The reviewer reproduced old socket cleanup erasing a newly reused descriptor's client membership; UUID ownership fixes it. Independent 16 native tests, strict concurrency, seven build-identity tests and packaged CLI checks passed. The listener is still not activated.
- Full component verification at `e13dab2` passed 512 Swift, 184 Python and nine packaging tests plus Release compilation. However, this run is NOT a clean-source qualification: the instrumented Release CLI emitted `default.profraw` during its argument-validation check, so the final manifest truthfully recorded `source_dirty=true`. Evidence: `/private/tmp/muesli-archive-components-verification`. The generated counter file was moved into that evidence directory. Release coverage is now explicitly disabled in project configuration and verification; profiling fallback output is confined to the evidence directory. Corrected qualification and independent review remain pending.
- Native screenshot copying, semantic prepare/finalize with private same-volume staging, and final listener/Quit activation are still being implemented/reviewed. No installed listener, client processing, public publication or Finder Trash action occurred.


## Native copies, Release correction and listener review checkpoint

- Screenshot copy chain `9cffb01` → `45faa17` is independently approved and integrated as `22a0353`, `ce7028c`. Exact copied bytes retain native source/asset/time provenance under source and destination ownership. Independent 57 tests passed, including the unchanged synchronization and damaged-PNG reproductions. PNG container/raster validation and durable reuse barriers now precede catalog admission.
- Coverage correction `703a1a2`, `a6da056` disables Release instrumentation and confines any fallback CLI profiling output to evidence. The resulting optimized build reproduced an Xcode Swift optimizer crash in screenshot candidate selection. `1309bf8` snapshots immutable sort keys without changing selection semantics. Independent review approved both corrections after 20 screenshot tests and optimized Release compilation. Root exact `1309bf845e793ffb0164741d0f879ec36cb04de1` Release passed with `source_dirty=false`, build ID `b3a957c72cd8c61f9e83fea97a433befcfd8d67313229d050f227e4ca30fb4e6`; packaged CLI signature and unsupported-command check passed in a fresh temporary working directory without emitting profile files. Evidence: `/private/tmp/muesli-archive-components-release-v3.log`. This is a Release check, not a new full combined qualification.
- Independent provenance review found the effective coverage setting was omitted from build identity. `343b2e3`, integrated as `da9e194`, adds it to the hashed compiler settings. Root independently ran all eight build-identity tests, including distinct YES/NO/absent coverage identities for the same clean source.
- Listener lifecycle `254168f` → `d284366` is independently approved and integrated as `7af1c95`, `75a37dc`. One retained constructor/close lane coalesces generations; accepted Quit retires admission synchronously, and Cancel waits for actual original closure before restart. Root reproduced stale failure publication after a successful retry, then replayed the exact regression green: 43 listener/workflow/Quit tests passed. Evidence: `/private/tmp/muesli-listener-independent-corrected-tests.log`. The listener is not yet activated.
- Integration review of the semantic adapter identified that its prepared context now retains real native file handles. Two independent regressions reproduced untracked cleanup after preparation-token release and idle Quit cleanup on MainActor. The generic workflow owner needs retained off-UI context disposal before activation. Evidence: `/private/tmp/muesli-workflow-independent-resource-red.log` (two tests, three expected failures before correction).
- A fresh audio review also identified delayed initial microphone startup surviving Stop/finalization. Source-intent retirement and native-to-UI adoption ownership are being corrected and require independent review. No hardware recording, installed-app replacement, actual Trash move or public publication occurred.
- Semantic prepare/finalize, native-context disposal and actual app activation remain under implementation/review. A new full combined qualification, final fresh whole-project Astra review, signed helper/package, clean installation/update/rollback and physical hardware acceptance remain open. Both specific approval requests remain pending.


## Native retirement and fresh audio review checkpoint

- Workflow context retirement `5a1e2d`, integrated as `d879807`, received independent approval after 49 tests. Native source/output owners now retire on a retained worker, including idle prepared-context cleanup during accepted Quit. Two unchanged regressions and actual file-descriptor closure gates confirm shutdown work survives until disposal finishes.
- Fixed native support bootstrap `bab708a`, integrated as `0393f8d`, received independent approval after five tests. Construction does no filesystem work; the retained listener worker creates only fixed private children without changing existing support-root permissions. The real support tree was inspected read-only; tests used temporary roots.
- Initial microphone ownership `2163311`, integrated as `19dd0b7`, received independent approval after 60 tests and unchanged extracted-production red/green startup scenarios. Stop synchronously retires source intent; original native startup stays reserved through UI adoption or actual cleanup. Independent test `989c35f`, integrated as `2a06bec`, also asserts original source-lease destruction occurs while shutdown ownership remains pending.
- Microphone health `29dd586`, integrated as `a759b01`, received independent approval after 48 focused tests and the unchanged actual-method stale-health reproduction. Receipt time now survives UI delivery; stale observations cannot reset recovery or publish healthy status, and Refresh checks freshness after suspensions. Evidence: `/private/tmp/muesli-mic-health-independent-tests.log`.
- App wiring `a8c57c8` received independent review that reproduced an old socket request entering reopened semantic admission after Cancel Quit. Correction `7d75c4c` moves reopening until all original listener clients/handlers have closed and a new startup token is admitted. Root replayed the exact original regression green: `/private/tmp/muesli-app-wiring-root-unchanged-replay.log`. Independent author validation passed 71 wiring/workflow/Quit tests, strict Swift 6 and optimized Release. Wiring integration still waits for the separately reviewed semantic source-admission correction.
- Semantic source admission review reproduced replacement PCM reaching an actual child before final rejection. Frozen correction `45598ef` passed 57 root tests including the unchanged reproduction and three additional staging/output-substitution tests. A follow-up is still required: archive initial snapshot admission must refuse a newly appeared pending transcript transaction without recovering it. No semantic approval or final integration is claimed yet.
- The fresh audio review also found known terminal system-capture failure could be hidden by previously converted frames before the queued UI error handler. That separate correction is in progress. Full combined verification and a final fresh integrated whole-project Astra review remain outstanding. Signed helper/distribution, clean installation/update/rollback and physical eight-hour/100-route/sleep gates remain open. No installed app, actual recording, real Trash or public publication occurred. Both specific approval requests remain pending.


## Combined archive wiring and remaining audio regressions

- Semantic adapter `e7a914f` and retirement fixture `e9eac8e` are now integrated as `7f314e8`, `98c4c2f`, after the original-source correction `45598ef` → `c121bc8` received independent approval and was integrated as `4debd8d`, `48e8f17`. Root's 68 tests include unchanged actual-child substitution and pending-recovery regressions plus independent private-staging/output-inode/post-move uncertainty cases. The independent Astra reviewer passed 76 tests, complete production/DEBUG strict checks and found no remaining P2+ within this admission slice.
- Reviewed app wiring and delayed semantic reopening are integrated as `91b1623`, `743bb40`. The AppModel merge retained microphone source retirement and inserted native archive accepted/cancel hooks. Independent staging regressions `18f08cf` and late-validation timeout regression `5d17980` are preserved as `1770bb1`, `be64c0a`; test-only append conflicts retained every test unchanged. Root integration checks passed 101 tests: `/private/tmp/muesli-archive-wiring-root-combined-tests.log` (63) and `/private/tmp/muesli-archive-wiring-root-cooperative-tests.log` (38).
- System terminal-health correction `449cdf0` is reviewed and integrated as `5b4182d`. Root replayed unchanged extracted production methods red on `19dd0b7` and green on the correction: retained native stop/conversion evidence precedes healthy publication, while legitimate silence remains allowed and repeated observation does not rearm recovery. Author full 559 tests, strict Swift 6 and clean identified optimized Release passed.
- Root's additional audio run exposed a previously intermittent microphone-ownership regression: the actual transferred offer source lease can be destroyed after its native work token is released. The strengthened `989c35f` deinit assertion is retained unchanged and is now a required red/green gate. Evidence: `/private/tmp/muesli-system-health-root-tests.log` (33 tests, one failure). The system-health delta is reviewed, but the combined audio run is explicitly NOT qualified.
- Fresh F7 review separately reproduced a stream-local mic write failure disabling later healthy system packets through the recorder's global admission flag. The source-specific failure correction is being implemented separately from shared manifest/store failure policy. No physical audio signal was used.
- Full combined verification, final fresh whole-project Astra review, signed helper/distribution, installation/update/rollback, participating installed archive build and physical eight-hour/100-route/sleep gates remain open. No installed app, user recordings or real Trash have been exercised. Both specific public-publication and helper-entitlement approvals remain pending.
